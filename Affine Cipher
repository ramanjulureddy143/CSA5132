#include<stdio.h>
#include<ctype.h>

int main(){
 char p[50];
 int a,b,i;

 printf("Text: "); gets(p);
 printf("a b: "); scanf("%d%d",&a,&b);

 for(i=0;p[i];i++)
  if(isalpha(p[i]))
   p[i]=((a*(toupper(p[i])-'A')+b)%26)+'A';

 printf("Cipher: %s",p);
}
